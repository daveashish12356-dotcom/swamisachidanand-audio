<?xml version="1.0" encoding="utf-8"?>
<!-- Nav pill: ek hi solid color – poora box same (no adha white adha color) -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- FULL nav box – pure white, top corners rounded, bottom flat -->
    <solid android:color="@color/card_background" />
    <corners
        android:topLeftRadius="@dimen/bottom_nav_pill_radius"
        android:topRightRadius="@dimen/bottom_nav_pill_radius"
        android:bottomLeftRadius="0dp"
        android:bottomRightRadius="0dp" />
</shape>
