<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/book_card_margin"
    app:cardCornerRadius="18dp"
    app:cardElevation="10dp"
    app:strokeWidth="0dp"
    app:cardBackgroundColor="@android:color/transparent"
    android:foreground="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:stateListAnimator="@null">

    <!-- Single 3D-style book cover with title like Shorts -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/book_card_thumbnail_height">

        <ImageView
            android:id="@+id/book_thumbnail"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="Book thumbnail"
            android:background="@drawable/book_placeholder" />

        <!-- Online badge (kept same ids/behaviour) -->
        <TextView
            android:id="@+id/book_online_badge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|end"
            android:layout_margin="8dp"
            android:background="@drawable/year_badge"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:text="ઓનલાઇન"
            android:textColor="@color/deep_saffron"
            android:textSize="11sp"
            android:textStyle="bold"
            android:visibility="gone" />

        <!-- Title overlay at bottom, YouTube Shorts style -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:orientation="vertical"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:paddingTop="8dp"
            android:paddingBottom="10dp"
            android:background="@drawable/bg_book_title_gradient">

            <TextView
                android:id="@+id/book_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="@android:color/white"
                android:maxLines="2"
                android:ellipsize="end"
                android:lineSpacingExtra="3dp"
                android:letterSpacing="0.02" />
        </LinearLayout>
    </FrameLayout>

    <!-- Hidden reading progress views to keep existing Java code compatible -->
    <LinearLayout
        android:id="@+id/reading_progress_layout"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:visibility="gone">

        <ProgressBar
            android:id="@+id/reading_progress_bar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:max="100"
            android:progress="0" />

        <TextView
            android:id="@+id/reading_progress_text"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:text="" />
    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
