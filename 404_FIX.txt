404 "There isn't a GitHub Pages site here" – ઠીક કરવા:

1. GitHub.com pe login → Create NEW repository
   Name: swamisachidanand-audio
   Public, README mat add karo.

2. Is folder (f:\ss\audio-repo) me terminal kholo:
   cd f:\ss\audio-repo
   git init
   git add .
   git commit -m "Audio list and docs"
   git branch -M main
   git remote add origin https://github.com/swamisachidanand/swamisachidanand-audio.git
   git push -u origin main

3. Repo → Settings → Pages
   Source: Deploy from a branch
   Branch: main, Folder: / (root)
   Save

4. 2–3 minute wait, phir open karo:
   https://swamisachidanand.github.io/swamisachidanand-audio/audio_list.json
   404 hat jayega, app ko list mil jayegi.

5. Audio play ke liye: Releases → Create release tag "amarakantak" → 24 WAV files upload (see RELEASE_UPLOAD_MAP.txt).
